
import {
  Box,
  Container,
  styled,
} from '@mui/material';
import HeroImg from '../assets/hero.jpg'
import Paper from '@mui/material/Paper';
import Typography from '@mui/material/Typography';

const HeroBackground = styled(Box)(({ theme }) => ({
  background: 'linear-gradient(135deg,rgb(85, 53, 41) 0%,rgb(227, 84, 84) 100%)',
  minHeight: '90vh',
  display: 'flex',
  alignItems: 'center',
  position: 'relative',
  overflow: 'hidden',
  '&::before': {
    content: '""',
    position: 'absolute',
    top: 5,
    right: 0,
    width: '100%',
    height: '100%',
    background: `url(${HeroImg}) center/cover`,
    opacity: 0.6,
  },
}));



function Hero() {
  return (
    <>
      <HeroBackground>
        <Container maxWidth="xl">
          <Box>
            <Paper sx={{ marginTop: 55, textAlign: 'center', padding: 2, opacity: 10 ,background: 'rgb(253, 253, 253)' }}>
              <Typography variant="h5" sx={{ fontFamily: 'ui-sans-serif', fontSize: 20, color: 'rgb(247, 68, 68)' }}>
                Sathi
              </Typography>
              <Typography variant="h3" sx={{ fontFamily: 'ui-sans-serif', fontSize: 35, fontWeight: 700, paddingBottom: 2 }}>
                No. 1 Matchmaking Service for People
              </Typography>

              <Typography variant="h6" gutterBottom sx={{ fontFamily: 'ui-sans-serif', color: 'rgb(243, 30, 30)' }}>
                Our matrimony service also comes with assured meetings, ensuring you can get in touch with the best matches and their families.
              </Typography>
            </Paper>
          </Box>

        </Container>
      </HeroBackground>

    </>
  );
}

export default Hero;
