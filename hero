import { Box, Container, Typography, Button, Grid } from '@mui/material';
import { styled } from '@mui/material/styles';
import ShieldIcon from '@mui/icons-material/Shield';
import ArrowForwardIcon from '@mui/icons-material/ArrowForward';
import HeroImg from '../../assets/hero.jpg';

const HeroWrapper = styled(Box)(({ theme }) => ({
  background: '#FFF5F5',
  minHeight: '100vh',
  position: 'relative',
  overflow: 'hidden',
  display: 'flex',
  alignItems: 'center',
}));

const LeftSection = styled(Box)(({ theme }) => ({
  padding: theme.spacing(8, 0),
  position: 'relative',
  zIndex: 2,
}));

const RightSection = styled(Box)(({ theme }) => ({
  position: 'absolute',
  right: 0,
  top: 0,
  width: '45%',
  height: '100%',
  background: `url(${HeroImg}) center/cover no-repeat`,
  '&::before': {
    content: '""',
    position: 'absolute',
    top: 0,
    left: 0,
    width: '100%',
    height: '100%',
    background: 'linear-gradient(to right, #FFF5F5 0%, rgba(255, 245, 245, 0.7) 20%, rgba(0,0,0,0.1) 100%)',
  },
}));

const TrustBadge = styled(Box)(({ theme }) => ({
  display: 'inline-flex',
  alignItems: 'center',
  gap: '8px',
  background: 'white',
  padding: '10px 24px',
  borderRadius: '50px',
  boxShadow: '0 2px 20px rgba(0,0,0,0.06)',
  marginBottom: theme.spacing(4),
}));

const CTAButton = styled(Button)(({ theme }) => ({
  padding: '14px 40px',
  borderRadius: '8px',
  fontSize: '1rem',
  fontWeight: 600,
  textTransform: 'none',
  boxShadow: 'none',
  '&:hover': {
    boxShadow: '0 10px 30px rgba(229, 57, 53, 0.2)',
    transform: 'translateY(-1px)',
  },
}));

function Hero() {
  return (
    <HeroWrapper>
      {/* Background Image - Absolute Positioned */}
      <RightSection />
      
      <Container maxWidth="xl" sx={{ position: 'relative', zIndex: 2 }}>
        <Grid container alignItems="center">
          <Grid item xs={12} md={7}>
            <LeftSection>
              {/* Trust Badge */}
              <TrustBadge>
                <ShieldIcon sx={{ color: '#E53935', fontSize: 20 }} />
                <Typography 
                  sx={{ 
                    color: '#666',
                    fontSize: '0.9rem',
                    fontWeight: 500,
                    letterSpacing: '0.5px'
                  }}
                >
                  India's Most Trusted Matrimony Service
                </Typography>
              </TrustBadge>

              {/* Main Heading */}
              <Typography
                variant="h1"
                sx={{
                  fontSize: { xs: '2.5rem', md: '3.5rem', lg: '4rem' },
                  fontWeight: 700,
                  color: '#1a1a1a',
                  lineHeight: 1.2,
                  mb: 2,
                  fontFamily: '"Georgia", "Times New Roman", serif',
                  maxWidth: '700px',
                }}
              >
                Your perfect match is just a click away
              </Typography>

              {/* Subheading */}
              <Typography
                sx={{
                  fontSize: { xs: '1.1rem', md: '1.2rem' },
                  color: '#666',
                  lineHeight: 1.8,
                  mb: 4,
                  maxWidth: '550px',
                  fontFamily: '"Inter", -apple-system, sans-serif',
                }}
              >
                Join millions who found their life partner on Sathi. 
                Intelligent matchmaking, verified profiles, and assured meetings.
              </Typography>

              {/* CTA Buttons */}
              <Box sx={{ display: 'flex', gap: 2, flexWrap: 'wrap' }}>
                <CTAButton
                  variant="contained"
                  sx={{
                    background: '#E53935',
                    '&:hover': { background: '#C62828' },
                  }}
                  endIcon={<ArrowForwardIcon />}
                >
                  Register Free
                </CTAButton>
                <CTAButton
                  variant="outlined"
                  sx={{
                    borderColor: '#ddd',
                    color: '#333',
                    borderWidth: '1.5px',
                    '&:hover': {
                      borderColor: '#E53935',
                      background: 'rgba(229, 57, 53, 0.04)',
                      borderWidth: '1.5px',
                    },
                  }}
                >
                  Explore Profiles
                </CTAButton>
              </Box>
            </LeftSection>
          </Grid>
          
          {/* Empty grid item to maintain layout spacing */}
          <Grid item xs={0} md={5} />
        </Grid>
      </Container>
    </HeroWrapper>
  );
}

export default Hero;
